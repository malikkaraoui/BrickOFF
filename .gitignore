# Hors projet — documents d'autres projets, jamais publiés
_hors_projet/

# Salle blanche — clones de repos tiers étudiés en lecture seule.
# Jamais commités : certains sont sans licence (tous droits réservés) ;
# on publie nos notes de conception, pas leur code.
/research/

# Données & modèles — trop lourds pour git, stockage externe documenté dans docs/ACCESS.md
data/raw/
data/processed/
*.pt
*.pth
*.onnx
*.mlpackage
*.sqlite
*.ckpt

# Secrets
.env
*.pem
*_key*
secrets/

# Python
.venv/
__pycache__/
*.egg-info/

# Xcode / macOS
.DS_Store
xcuserdata/
*.xcuserstate
build/
DerivedData/
.gstack/
ml/audit_grid_*.html

# Artefacts ML générés localement (reproductibles — les preuves committées vivent dans ml/runs/)
ml/runs/*.log
ml/synth_*.png
ml/*_grid.html
ml/spot_the_fake/
.venv-labelstudio/
.claude/

# Photos brutes PO (données, hors git — trop lourdes) + images de contrôle jetables
lego photos*/
photos Lego*/
ml/verif_*.png
ml/batch2_*.png
ml/piles_*.png
ml/*_render.png
ml/*_check.png
ml/synth_v2*_smoke*
